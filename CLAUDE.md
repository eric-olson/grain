# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Build & Run

```bash
cargo build --release        # Build optimized binary → target/release/grain
cargo run --release          # Build and run
cargo build                  # Debug build (faster compile, slower runtime)
```

No test suite yet — validation is done manually using files in `testdata/` (generated by `testdata/generate.py`).

## Architecture

Rust GUI application for visualizing binary files, built for satellite radio data reverse-engineering. Uses **eframe/egui** (immediate-mode GUI) with **memmap2** for memory-mapped file access.

### Module Overview

- **`main.rs`** — Entry point. Creates eframe window and launches `App`.
- **`app.rs`** — Core state machine implementing `eframe::App`. Owns all UI layout: menu bar, status bar, hex panel, search panel, viewport, and stride detection dialog. Manages background task polling each frame.
- **`file_handler.rs`** — `MappedFile` wrapper around `memmap2::Mmap`. Provides `get_range()` for bounded slice access.
- **`viewer.rs`** — `PixelGridViewer` renders binary data as a 2D pixel grid. Two display modes: Byte (grayscale) and Bit (B/W per bit). Builds a `ColorImage` on CPU, uploads as an egui texture, and caches until state changes.
- **`stride_detect.rs`** — Stride detection via byte/bit-match periodicity. Counts exact matches (`data[i] == data[i+stride]`) across candidate lags, uses z-scores to find statistically significant periodicities, and filters harmonics to prefer fundamental frequencies. Supports both byte and bit modes.
- **`sync_search.rs`** — Hex pattern search with automatic variation generation (bit-inverted, bit-reversed, byte-swapped). Linear scan.

### Key Patterns

- **Threading**: Heavy operations (search, stride detection) run on `std::thread::spawn` with `Arc<Mutex<>>` for result sharing. Main thread polls each frame.
- **Viewport rendering**: Only visible data is rendered. Texture is rebuilt only when offset/stride/zoom/highlights change.
- **Bit-addressability**: Stride and display can operate at bit granularity for non-byte-aligned satellite protocols.
- **Data flow**: `MappedFile` → `Arc<Vec<u8>>` cached copy → shared with background threads and `PixelGridViewer`.

### Design Document

`design.md` (not checked in) contains the full architectural vision including planned features beyond the current MVP.

## Style Guide

See `STYLE.md` for Rust conventions, patterns, and best practices used in this codebase.
